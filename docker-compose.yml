version: "3.9"

services:
  lora-map:
    build: .
    container_name: lora-map
    ports:
      - "4000:4000"
    volumes:
      # Verknüpft den lokalen Ordner "data" mit dem Ordner im Container
      - ./data:/app/data
    restart: unless-stopped
